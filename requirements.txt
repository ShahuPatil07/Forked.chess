# Core chess / engine interface
chess>=1.10.0

# HTTP requests (Chess.com / Lichess APIs)
requests>=2.31.0

# Progress bars
tqdm>=4.66.0

# Visualisation
matplotlib>=3.8.0

# Numerical
numpy>=1.26.0

# Data serialisation (parquet read/write for training data)
pyarrow>=15.0.0

# Model serialisation
joblib>=1.3.0

# ML - Stage 1 threat classifier
lightgbm>=4.3.0

# ML - Stage 2 (clustering pipeline)
scikit-learn>=1.4.0        # includes sklearn.cluster.HDBSCAN (no separate hdbscan needed)
umap-learn>=0.5.6

# LLM labelling (cluster names via Groq free tier — set GROQ_API_KEY env var)
groq>=0.9.0

# Stage 3: Lichess puzzle DB decompression (.zst format)
zstandard>=0.22.0

# Backend API
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Optional: Maia2 human-move probability model (requires PyTorch)
# Install separately if needed: pip install maia2
# Falls back gracefully when unavailable (USE_MAIA2=False in ml/config.py)

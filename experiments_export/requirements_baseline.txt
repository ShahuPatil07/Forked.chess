# Baseline deps to reproduce the current Stage 1 + Stage 2 models.
# Your new transformer / CSAE experiments add torch (and optionally maia2).
python-chess>=1.10.0
numpy>=1.24
pyarrow>=14.0
scikit-learn>=1.4.0
lightgbm>=4.3.0          # Stage 1 baseline
joblib>=1.3
umap-learn>=0.5.6        # Stage 2 baseline (UMAP-16)
zstandard>=0.22          # build_training_data.py streams the .zst puzzle DB
tqdm>=4.66

# Experiment additions (uncomment as needed):
# torch                  # transformer (Stage 1) + CSAE (Stage 2) — CPU build via the pytorch index
# maia2>=0.9             # Stage 2 dense embeddings, if you go that route
# groq>=0.9.0            # only to re-run the Stage 2 baseline LLM labeller
